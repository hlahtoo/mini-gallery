// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["picsum.photos", "upload.wikimedia.org", "peach.blender.org"],
  },
};

module.exports = nextConfig;
